[pytest]
minversion = 8.0
testpaths = tests
pythonpath = .
asyncio_mode = auto
# Coverage threshold lowered to 55% due to removal of API key related tests
addopts = --strict-markers --cov=src --cov-report=html --cov-report=term-missing --cov-fail-under=55

